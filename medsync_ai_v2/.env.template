# Default provider used unless an agent specifies its own
LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4-5-20250929

# API Keys (both required for dual-provider support)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Per-agent overrides (optional — agents fall back to default above)
# Format: AGENT_<NAME>_PROVIDER and AGENT_<NAME>_MODEL
# AGENT_CHAIN_OUTPUT_AGENT_PROVIDER=openai
# AGENT_CHAIN_OUTPUT_AGENT_MODEL=gpt-4.1
# AGENT_QUERY_CLASSIFIER_PROVIDER=anthropic
# AGENT_QUERY_CLASSIFIER_MODEL=claude-sonnet-4-5-20250929

# Firebase
FIREBASE_CRED_PATH=./medsyncai.json
FIREBASE_COLLECTION=search_database
